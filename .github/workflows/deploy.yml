name: Deploy Static Site

on:
  push:
    branches: [ "main" ]   # Se ejecuta cada vez que haces push a la rama master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Deploy a GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.MYSECRET_TOKEN }}
          publish_branch: gh-pages   # 👈 asegúrate que publique a gh-pages
          publish_dir: ./            # 👈 carpeta donde está tu index.html